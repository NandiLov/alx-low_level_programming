#include "main.h"
/**
* int mul - multiply integers a and b
* @a: first integer
* @b: second integer
* @mul: is a * b
* Return: 0
*/
int mul(int a, int b)
{
	int a;
	int b;
	int mul = a * b;

		{
		printf("%d * %d = %d\n", a, b, int mul);
		return (0);
		}
}
